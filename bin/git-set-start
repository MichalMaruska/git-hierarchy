#!/bin/bash -eu

# set the reference to the SHA of the Commit.
# sort of git-tag, but...
ref=$1
commit=$2

# wrong?
sha=$(git show-ref --hash $commit)

# update
# git symbolic-ref refs/test base/aaa
git update-ref $ref $sha
